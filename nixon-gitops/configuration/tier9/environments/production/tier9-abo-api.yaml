nameOverride: tier9-abo-api

dotNetEnvironment: Production

image: "sharedpersonalcontaineregistry.azurecr.io/abo.api:master-20260804-205957-1fbe5f9f"

resources:
  requests:
    memory: 40Mi
    cpu: 50m
  limits:
    memory: 140Mi
    cpu: 500m

extraEnvValues:
  - name: PG_PASSWORD
    externalSecretKeyRef:
      key: ABO-PG-USER-WEB

containerPorts:
  http:
    enabled: true

service:
  enabled: true
  ports:
    http:
      enabled: true

ingress:
  enabled: true
  rules:
    - host: api.autobattles.online
      paths:
        - path: /
          pathType: Prefix

hpa:
  enabled: true
