{{- if .Values.objectStore.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "cnpg-cluster.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "cnpg-cluster.labels" $ | indent 4 }}
spec:
  groups:
    - name: cnpg.cluster.backups
      rules:
        - alert: CNPGBackupFailed
          expr: |
            max by (namespace, cluster) (
              barman_cloud_cloudnative_pg_io_last_failed_backup_timestamp{
                namespace="{{ .Release.Namespace }}",
                cluster="{{ include "cnpg-cluster.clusterName" . }}"
              }
            )
            >
            max by (namespace, cluster) (
              barman_cloud_cloudnative_pg_io_last_available_backup_timestamp{
                namespace="{{ .Release.Namespace }}",
                cluster="{{ include "cnpg-cluster.clusterName" . }}"
              }
            )
          for: 5m
          annotations:
            summary: "CNPG backup failed for {{ include "cnpg-cluster.clusterName" . }}"
            description: "The most recent backup for {{ include "cnpg-cluster.clusterName" . }} failed and no newer successful backup has been recorded."
{{- end }}
